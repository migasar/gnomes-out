<div class="container">
  <div class="margin-navbar">
      <div class="container">

        <div class="first-title mb-4"><h1>Resultats recherche</h1></div>

        <%= render 'shared/search-subcategory' %>

        <div class="mapbox" id="map"
        style="width: 100%; height: 600px;"data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
        <div class="card-all-gnomes">
          <% @gnomes.each do |gnome|%>
            <%= link_to(gnome_path(gnome)) do %>
              <div class="card-polaroid">
                <div class="polaroid">
                  <div class="polaroid-img" style="background-image:  url(<%= gnome.photo %>)" >
                  </div>
                  <div class="caption">
                    <h3><%=  gnome.name %></h3>
                    <p><strong><%= gnome.price %></strong>€ par jour</p>
                  </div>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
  </div>
</div>
