<div class="container">
  <div class="margin-navbar">
      <div class="container">
        <div class="first-title mb-4"><h1>Gnomes <%= @curent_category %></h1></div>
        <div class="card-all-gnomes">
          <% @gnomes.each do |gnome|%>
              <div class="card-product">
                <img src="<%= gnome.photo %>" />
                <div class="card-product-infos">
                  <h2><%= link_to gnome.name, gnome_path(gnome) %></h2>
                    <div class="adopt"><p>Adoptez-moi !</p></div>
                  <p><strong><%= gnome.price %></strong>€ par jour</p>
                </div>
              </div>
          <% end %>
        </div>
      </div>
  </div>
</div>
